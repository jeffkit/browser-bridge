{
  "name": "browser-bridge",
  "version": "0.1.0",
  "private": true,
  "description": "Remote agent ↔ local browser bridge: MV3 extension + MCP gateway",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
