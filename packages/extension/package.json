{
  "name": "@browser-bridge/extension",
  "version": "0.3.0",
  "private": true,
  "description": "browser-bridge Chrome/Edge MV3 extension: outbound WebSocket to gateway, exposes browser control to remote agents",
  "dependencies": {
    "@browser-bridge/protocol": "workspace:*"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0"
  }
}
