{
  "name": "@browser-bridge/protocol",
  "version": "0.3.0",
  "private": true,
  "description": "Shared wire protocol types for browser-bridge (extension ↔ gateway)",
  "type": "module",
  "license": "MIT",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jeffkit/browser-bridge.git",
    "directory": "packages/protocol"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
